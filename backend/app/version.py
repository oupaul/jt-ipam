"""後端版本字串（單一來源）。

發版時請與 frontend/package.json 的 version 一起更新。
"""

__version__ = "0.4.188"
>>>>>>> a7eeaea (chore(agent): make mDNS/avahi opt-in (JT_IPAM_ENABLE_MDNS) so the installer no longer pulls avahi-daemon by default [v0.4.188])
